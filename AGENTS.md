

when working on jupyter notebook cells,
always check that the environment is activated first,
write the objective/plan. then start with the first task. 
do not move to the next task until the code cells created for the task is ran.
always read through the output for each code cell to check if it meets the objective.
if the code cell requires import libraries that are not present, do not install or replace libaries; ask for help.